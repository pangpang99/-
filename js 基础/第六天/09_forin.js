/*var person={
 name:'tom',
 age:18,
 sex:'男'
};

for(var key in person){
   //key属性名
	 console.log(key,person[key]);
}
*/
//练习：创建对象。包含若干个人成绩属性。遍历对象中属性获取总成绩。
//提升声明sum用于保存总和
var grade={
   chinese:80,
   math:840,
	 englist:80,
	 history:89
 };
	 //var num=0
 for(var key in grade){
	 console.log(grade,grade[key]);
   //num+=grade[key]
 }
//console.log(num);









